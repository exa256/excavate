import { Wallet } from "ethers";
import { networkSettings, Network } from "../../src/networks";

export const forkBlock = null
// export const signer: Wallet = new Wallet('')
export const network: Network = networkSettings
.ethereum
// .polygon
// .optimism
// .arbitrum
